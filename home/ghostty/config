{
  theme = "Violet Light";
  # Don't hang around
  quit-after-last-window-closed = true;

  # Set default shell to nix-fish
  command = "/run/current-system/sw/bin/fish";

  keybind = [
    "ctrl+left=esc:b"
    "ctrl+right=esc:f"
  ];
}
